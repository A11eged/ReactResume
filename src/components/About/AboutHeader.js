import React from 'react';
import styles from './AboutHeader.module.css';

const AboutHeader = () => {
  return (
    <div className={styles.aboutHeader}>
      <h1 className={styles.AboutHeaderTitle}>About</h1>
    </div>
  );
};

export default AboutHeader;
